## 205: If/Else

```go
if cond {
    // do something
} else if {
    // do something else
} else {
    // last choice
}

// assignment before predicate
if x := 10; x < 20 {
    fmt.Printf("x is %d which is less than 20", x)
}
```
